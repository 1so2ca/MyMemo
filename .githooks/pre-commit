#!/bin/sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "commit blocked: gitleaks is not installed or not on PATH" >&2
  exit 1
fi

.githooks/check-forbidden-files staged

exec gitleaks git --pre-commit --redact --staged --verbose
